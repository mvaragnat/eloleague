<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || t('app.name', default: 'Eloleague') %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="description" content="<%= t('app.description') %>">

    <meta property="og:type" content="website">
    <meta property="og:site_name" content="<%= t('app.name', default: 'Eloleague') %>">
    <meta property="og:title" content="<%= content_for(:title) || t('app.name', default: 'Eloleague') %>">
    <meta property="og:description" content="<%= t('app.description') %>">
    <meta property="og:image" content="<%= "#{request.base_url}/og-image.png" %>">
    <meta property="og:image:width" content="1200">
    <meta property="og:image:height" content="630">
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="<%= content_for(:title) || t('app.name', default: 'Eloleague') %>">
    <meta name="twitter:description" content="<%= t('app.description') %>">
    <meta name="twitter:image" content="<%= "#{request.base_url}/og-image.png" %>">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :application, "data-turbo-track": "reload" %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr@4.6.13/dist/flatpickr.min.css">
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="site-nav">
      <% if user_signed_in? %>
        <%= link_to t('home.link'), dashboard_path, class: "brand" %>
      <% end %>
      <% if user_signed_in? || admin_signed_in? %>
        <%= link_to t('stats.title', default: 'Stats'), stats_path, class: "brand" %>
      <% end %>
      <%= link_to t('tournaments.index_title'), tournaments_path, class: "brand" %>
      <%= link_to t('championship.nav_link'), championships_path, class: "brand" %>
      <span class="spacer"></span>
      <% if user_signed_in? %>
        <span class="user-email" style="font-weight:600; margin-right: .75rem; color: var(--text-muted);">
          <%= current_user&.email %>
        </span>
        <%= link_to t('auth.logout', default: 'Logout'), destroy_user_session_path, data: { turbo_method: :delete }, class: "brand" %>
      <% else %>
        <%= link_to t('auth.login', default: 'Sign in'), new_user_session_path, class: "brand" %>
        <%= link_to t('auth.signup', default: 'Sign up'), new_user_registration_path, class: "brand" %>
      <% end %>
      <span style="margin-left:0.75rem;"></span>
      <%= link_to "🇬🇧", url_for(locale: :en), class: "brand" %>
      <%= link_to "🇫🇷", url_for(locale: :fr), class: "brand" %>
    </nav>
    <main>
      <% if alert = flash[:alert] %>
        <div class="flash-container">
          <div class="flash flash--alert" role="alert">
            <span class="flash-icon">⚠️</span>
            <span><%= alert %></span>
          </div>
        </div>
      <% end %>
      <% if notice = flash[:notice] %>
        <div class="flash-container">
          <div class="flash flash--notice" role="status">
            <span class="flash-icon">✅</span>
            <span><%= notice %></span>
          </div>
        </div>
      <% end %>
      <%= yield %>
    </main>
    <%= render FooterComponent.new %>
  </body>
</html>
